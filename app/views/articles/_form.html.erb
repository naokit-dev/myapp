
  <% if article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% article.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="editor-form container-fluid">
    <%= bootstrap_form_with(model: @article, local: true) do |f| %>

    <div class="row" id="editor">
    <div class="col-md-4 order-md-2">
    <h1 class="editor-form"><%= yield(:form_title) %></h1>
    <%= f.submit yield(:btn_name), class: 'btn btn-primary form-control' %>
    </div>
      <div class="col-md-8 order-md-1" >
        <%= f.text_area :title, label: "Abstract",
                            autofocus: true,
                            placeholder: '気づきや体験を140文字以内で書こう',
                            id: 'article_title' %>
      </div>

      <div class="col-md-6  order-md-3">
        <%= f.text_area :contet, class: 'editor_parts', id: 'article_content',
                          placeholder: '本文',
                          "v-model" => "input", name: "article[content]" %>
      </div>

      <div class="col-md-6  order-md-4">
        <label>Preview</label>
        <div v-html='input | marked' class="editor_parts form-control overflow-auto" id="preview_area">
        </div>
      </div>
      <div class="col-12">
      </div>
      <% end %>
    </div>
  </div>

<script type="text/javascript">
  window.onload = function() {
    new Vue({
      el: '#editor',
      data: {
        input: '<%== j@article.content %>',
      },
      filters: {
        marked: marked,
      },
    });
  };
</script>

