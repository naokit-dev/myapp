| <div class="modal fade" id=
= "#{id}"
|  tabindex="-1" role="dialog" aria-labelledby=
= "#{id}CenterTitle"
|  aria-hidden="true"> 
.modal-dialog.modal-dialog-centered[role="document"]
  .modal-content
    .modal-header
      |  <h5 class="modal-title" id=
      = "#{id}CenterTitle"
      | >Please enter article password
      button.close[type="button" data-dismiss="modal" aria-label="Close"]
        span[aria-hidden="true"]
          | ×
    - if id == "edit-modal" then
      - url = edit_article_path(@article)
      - method = :get
    - elsif id == "destroy-modal" then
      - url = article_path(@article)
      - method = :delete
    = bootstrap_form_with(url: url, method: method, local: true) do |f|
      .modal-body
        = f.password_field  :article_token, autofocus: true, hide_label: true, placeholder: "article password"
      .modal-footer
        button.btn.btn-secondary[type="button" data-dismiss="modal"]
          | Close
        = f.submit "Enter", class: 'btn btn-primary'
