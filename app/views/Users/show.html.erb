<div class="container-fluid" id="user_show">
    <div class="row">
        <div class="col-md-4">
            <h4><i class="far fa-id-card"></i> Profile</h4>
            <div class="card h-100">
                <p>プロフィールを表示</p>
            </div>
        </div>
        <div class="col-md-8">
            <nav class="navbar">
            <h4><i class="fab fa-gratipay"></i> Favorite articles</h4>
            <ul class="nav justify-content-end">
                <li class="navitem">
                    <%= link_to "記事一覧", "#" %>  
                </li>
            </ul>
            </nav>
            <%= render @articles %>
            <%= paginate @articles %>
            <nav class="navbar">
            <h4><i class="fas fa-history"></i> Most recent articles</h4>
            <ul class="nav justify-content-end">
                <li class="navitem">
                    <%= link_to "記事一覧", "#" %>  
                </li>
            </ul>
            </nav>
            <%= render @articles %>
            <%= paginate @articles %>
        </div>
    </div>
</div>



